//import { computedStyle } from './computedStyle';

test('can resolve styles', () => {
  /*
  const node = document.createElement('div');
  expect(typeof computedStyle(node)).toBe('object');
  */
  expect(true).toBe(true);
});